import { z } from 'zod'

/**
 * PLACEHOLDER — PMT-006 replaces this with `validateReadings()` from
 * src/utils/PermitSafety.ts against `safetyReadings` + `outdoorWork`. See
 * Step1Type.schema.ts for why `z.object({})` is the correct placeholder shape.
 */
export const Step3SafetyChecksSchema = z.object({})
