import { z } from 'zod'

/**
 * PLACEHOLDER — PMT-005 replaces this with real checks on project/foreman/date/
 * time/description/location. See Step1Type.schema.ts for why `z.object({})`
 * is the correct placeholder shape (always passes, unblocks Next end to end).
 */
export const Step2BasicInfoSchema = z.object({})
