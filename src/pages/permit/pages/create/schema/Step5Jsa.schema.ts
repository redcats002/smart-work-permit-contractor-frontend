import { z } from 'zod'

/**
 * PLACEHOLDER — PMT-008 replaces this with a check that every JSA phase has at
 * least one step in `jsaSteps`. See Step1Type.schema.ts for why `z.object({})`
 * is the correct placeholder shape.
 */
export const Step5JsaSchema = z.object({})
