import { z } from 'zod'

/**
 * PLACEHOLDER — PMT-007 replaces this with checks on photo evidence + the
 * `workers` table (role, health check). See Step1Type.schema.ts for why
 * `z.object({})` is the correct placeholder shape.
 */
export const Step4PpeWorkersSchema = z.object({})
