# แก้ไข Menu Permission - แสดง submenu ตาม permission

## ปัญหา
- Staff role มี `tasks: ['read_collection']` แต่ไม่สามารถเห็นเมนู "ติดตามทวงถาม" ได้
- สาเหตุ: Router ไม่ได้ใส่ `permissionAction` ใน child routes
- ปัจจุบัน: เมนู "งาน" แสดงถูก แต่ submenu "ติดตามทวงถาม" ไม่แสดงสำหรับ staff

## Requirement ใหม่
- เมนูที่เคยมี submenu มากกว่า 1 แล้วถูกกรองเหลือ 1 → ยังแสดง dropdown (มี item เดียว)
- เมนูที่มี submenu แค่ 1 ตั้งแต่แรก → แสดงเป็น simple menu item (ไม่มี dropdown)

## วิธีแก้ไข

### ไฟล์: `src/components/app/AppDrawer.vue`
**ส่วนที่ 1: บรรทัด 127-150** - เพิ่มตัวนับ children ต้นฉบับ (ก่อนกรอง) เพื่อตัดสินใจว่าจะแสดง dropdown หรือ simple item

ปัจจุบัน:
```typescript
const fullPath = normalizePath('', route.path)
const childMenuItems = (route.children ?? []).flatMap((child: RouteRecordRaw): IMenuRouteItem[] => {
  const childPath = normalizePath(fullPath, child.path)
  return collectMenuRouteItems(child, childPath)
})

if (childMenuItems.length === 0) {
  return []
}

const icon = routeMeta.icon ?? childMenuItems[0]?.icon ?? ''
const key = String(route.name ?? route.path)

if (childMenuItems.length === 1) {
  return [
    {
      label: (routeMeta?.menuTitle || routeMeta?.title) ?? childMenuItems[0].label,
      icon,
      key,
      notify: getNotifyFlag(route),
      to: childMenuItems[0].to
    }
  ]
}

return [
  {
    label: (routeMeta?.menuTitle || routeMeta?.title) ?? childMenuItems[0].label,
    icon,
    key,
    notify: getNotifyFlag(route),
    children: childMenuItems.map((item: IMenuRouteItem): ISubMenuItem => ({
      label: item.label,
      to: item.to,
      disabled: item?.disabled
    }))
  }
]
```

แก้ไขเป็น:
```typescript
const fullPath = normalizePath('', route.path)

// นับ children ต้นฉบับที่มี menu: true (ก่อนกรอง permission)
const originalChildCount = (route.children ?? []).filter((child: RouteRecordRaw): boolean => {
  const childMeta = getRouteMeta(child)
  return !!childMeta.menu
}).length

const childMenuItems = (route.children ?? []).flatMap((child: RouteRecordRaw): IMenuRouteItem[] => {
  const childPath = normalizePath(fullPath, child.path)
  return collectMenuRouteItems(child, childPath)
})

if (childMenuItems.length === 0) {
  return []
}

const icon = routeMeta.icon ?? childMenuItems[0]?.icon ?? ''
const key = String(route.name ?? route.path)

// ถ้าต้นฉบับมีแค่ 1 → simple item, ถ้าต้นฉบับมีมากกว่า 1 → dropdown (แม้จะเหลือ 1)
if (originalChildCount <= 1) {
  return [
    {
      label: (routeMeta?.menuTitle || routeMeta?.title) ?? childMenuItems[0].label,
      icon,
      key,
      notify: getNotifyFlag(route),
      to: childMenuItems[0].to
    }
  ]
}

return [
  {
    label: (routeMeta?.menuTitle || routeMeta?.title) ?? childMenuItems[0].label,
    icon,
    key,
    notify: getNotifyFlag(route),
    children: childMenuItems.map((item: IMenuRouteItem): ISubMenuItem => ({
      label: item.label,
      to: item.to,
      disabled: item?.disabled
    }))
  }
]
```

### ไฟล์: `src/router/modules/work/index.ts`
เพิ่ม `permissionAction` ใน child routes (ทำแล้ว):
- `asset-appraisal`: `permission: 'tasks', permissionAction: 'read_asset_valuation'`
- `follow-up`: `permission: 'tasks', permissionAction: 'read_collection'`

## ผลลัพธ์ที่ได้

### เมนู "งาน" (originally 2 children)
| Role | Tasks Permission | ผลลัพธ์ |
|------|-----------------|---------|
| STAFF | `['read_collection']` | งาน → dropdown: ติดตามทวงถาม |
| SUPERVISOR+ | `['read_asset_valuation', 'read_collection']` | งาน → dropdown: ประเมินหลักทรัพย์ + ติดตามทวงถาม |

### เมนูอื่นๆ (originally 1 child)
- ยังแสดงเป็น simple menu item เหมือนเดิม

## ตรวจสอบ
1. รัน `bun run dev`
2. Login ด้วย role STAFF
3. ตรวจสอบว่าเห็นเมนู "งาน" → dropdown มี "ติดตามทวงถาม"
4. Login ด้วย role SUPERVISOR
5. ตรวจสอบว่าเห็นเมนู "งาน" → dropdown มี "ประเมินหลักทรัพย์" + "ติดตามทวงถาม"
6. ตรวจสอบว่าเมนูอื่นๆ ที่มี child เดียวยังแสดงเป็น simple item
